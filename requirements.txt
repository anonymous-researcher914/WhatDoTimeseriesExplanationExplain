numpy>=1.24
scipy>=1.10
pandas>=2.0
torch>=2.0
pyyaml>=6.0
matplotlib>=3.7  # experiments/run_var_omic.py's Block B1 figures
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# experiments/comparison_realdata*.py: KARMA/IG/TimeShap/TS-MuLe/ShapTime/TIMING/TEIG comparison
captum>=0.6
timeshap>=1.0
# shap>=0.43: timeshap 1.0.4 imports shap's private `Kernel` class, renamed to the public
# `KernelExplainer` in shap 0.43 (run_timeshap aliases it back in at runtime for exactly this
# reason). shap<0.43 still has `Kernel` but predates NumPy 2 support and crashes on import here.
shap>=0.43
stumpy>=1.12   # tsmule's MatrixProfileSegmentation
dill>=0.3      # tsmule
pyts>=0.13     # tsmule
# ts-mule itself is not on PyPI -- clone github.com/dbvis-ukon/ts-mule into ./ts-mule
# (experiments/comparison_realdata.py's own sys.path insertion expects it there)
